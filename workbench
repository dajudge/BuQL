#! /bin/sh

set -e

DIR=$(dirname "$(readlink -f "$0")")
echo $DIR

IMG=$(docker build -q $DIR/.workbench)

if [ "$#" -eq 0 ]; then
    CMD=bash
else
    CMD="$@"
fi

if [ "true" = "$CI_MODE" ]; then
    CONTAINER_OPTS="-v /root:/root"
else
    CONTAINER_OPTS="-e USER=$USER -v $HOME:$HOME --entrypoint entrypoint-interactive.sh -it"
fi

echo docker run --rm \
    -v /etc/passwd:/etc/passwd \
    -v /etc/group:/etc/group \
    -w $DIR \
    --net host \
    $CONTAINER_OPTS $IMG $CMD


docker run --rm \
    -v /etc/passwd:/etc/passwd \
    -v /etc/group:/etc/group \
    -w $DIR \
    --net host \
    $CONTAINER_OPTS $IMG $CMD
